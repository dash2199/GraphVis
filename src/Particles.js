import React from 'react'
import Parts from 'react-particles-js'
import PartConfig from './particleconfig'

export default function Particles(){
    return (
        <Parts params = {PartConfig}></Parts>
    );
}
